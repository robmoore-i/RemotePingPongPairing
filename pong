if [[ $# != "1" ]]
then
  echo "USAGE: $0 <your partner's name>"
  exit 1
fi

branch_name="remote-wip-$1"
git fetch origin $branch_name
git rebase $branch_name
git branch -d $branch_name
git push --delete $branch_name
